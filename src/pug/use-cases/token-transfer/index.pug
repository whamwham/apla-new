include /pug/includes/common.puginclude /pug/includes/data.pug-    var currPage = $i18n.USE_CASES[8];    htmlAttrs = currPage.htmlAttrs;include /pug/includes/headbody(class=$i18n.lang)    include /pug/includes/header-main    use-case        .screen-use-case            .container                h1!=currPage.title                section                    .left                        each value, index in currPage.benefits                            +use-case-benefit-list-item(value)(value)                    .right                        .video.video-section                            .video-wrapper                                video-js#singleVideo(preload='no' crossorigin='' poster="/assets/images/video-bkg-sm-t.svg" controls=true data-setup='{"fluid": true }')                                    source(src!=currPage.video)                        .descr!=currPage.descr    use-cases        .screen-use-cases            .container                h2 Use cases                section                    each item in $i18n.USE_CASES                        +use-cases-main-list-item(item)        include /pug/includes/newsletteralt        include /pug/includes/footer        include /pug/includes/popupform